// Configuration file - API URLs
// For Vercel deployment, use empty string (relative paths)
// For local development with Vercel CLI: use empty string
// For local development with separate backend: use 'http://localhost:3001'
window.API_URL = window.API_URL || '';

